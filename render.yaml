services:
  - type: web
    name: markov-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
  # All-in-one expected-move page — ES/NQ/YM/RTY ranked by how far each has
  # travelled vs its own daily EM (who's leading), with each market's daily
  # S/R levels and the paste-able daily/weekly/monthly EM numbers. Keeps the
  # original service name so the existing URL still works. (uvicorn four_em:app)
  - type: web
    name: nq-expected-move
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn four_em:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
